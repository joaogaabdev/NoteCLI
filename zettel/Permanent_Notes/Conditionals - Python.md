Referees: #Python 

``` python
idade = 18

if idade > 18 #Se a idade for maior que 18, execute

if idade >= 18 #Se a idade for maior OU igual a 18, execute

if idade == #Se idade for 18, execute

if idade < 18 #Se a idade for menor que 18, execute

if idade <= 18 #Se a idade for menor OU iguak a 18, execute

elif #Outra condição 

else: #Senão
```

# Operador ternário

compara se um bloco de código com outro caso seja verdadeiro ou falso

``` python
idade = 18

mensagem = "Pode tirar a carteira de habilitação" if idade >= 18 "Você não pode tirar a carteira de habilitação"

print(mensagem) #Se a idade for maior OU igual a 18(TRUE), então mensagem recebe a string "Pode tirar a carteira de habilitação"
```