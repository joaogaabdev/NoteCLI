### Comandos SQL

Para criar uma tabela no PostgreSQL, basta:
```postgresql
CREATE TABLE produtos;
```

``;`` -> Indica o fim de um comando SQL.

#### Boas práticas:
Comandos em letra maiúscula e nomes de variáveis e campos em letra minúscula.
