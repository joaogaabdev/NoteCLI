#Frontend 
### Document Object Model (DOM) 

The Dom have a series of objects each representating a single HTML element

`DOM => HTML element`

It means all the elements in the HTML file are represented as objects in document object model



