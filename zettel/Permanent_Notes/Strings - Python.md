Referees: #Python 
``` python
nome = "Joao"

nome.upper() #Método para deixar as strings maiúsculas
nome.lower() #Método para deixar as strings minúsculas 
nome.count() #Método para contar quantas letras existe, naquela string,
#ex: nome.count("a") -> Resultado vai ser 1
nome.find() #Método para encontrar a posição da string que eu quero
nome.enconde() #Método para transformar a string em bytes
nome.replace() #Método para substituir 
#ex: nome.replace("a", "e") -> substitui todas as ocorrências de a por e
#Resultado Joeo

```


